apiVersion: v1
kind: Service
metadata:
  name: multi-agent-devops
  namespace: ai-platform
  labels:
    app.kubernetes.io/name: multi-agent-devops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: multi-agent-devops
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
